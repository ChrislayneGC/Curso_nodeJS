const os = require("os");

console.log(os.cpus()); //Quantos CPUs tem o servidor

console.log(os.freemem()); //Quantas Memórias Livres tem na maquina

console.log(os.homedir()); //Qual diretório da Home

console.log(os.type()); //Qual o tipo de sistema operacional